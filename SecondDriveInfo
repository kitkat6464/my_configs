Create mnt mount folder: sudo mkdir /mnt/games

Look for UUID: lsblk -f

Add this to /etc/fstab

UUID=THEUUID /mnt/games     btrfs   defaults,noatime,compress=zstd,commit=120 0 0

SAVE

Then run: sudo systemctl daemon-reload

And then run: sudo mount -a
